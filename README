UniShare
by Rich Jones, Alec Story and Thomas Levine
for FreeCulture.org

If you pull this from GitHub, you may need to move the parent directory
from UniShare to unishare.

Reqs:
svn checkout http://django-tagging.googlecode.com/svn/trunk/ django-tagging-read-only
sudo python setup.py install

svn checkout http://django-simple-captcha.googlecode.com/svn/trunk/ django-simple-captcha-read-only
sudo python setup.py install

To run the server:
`python manage.py syncdb`
`python manage.py runserver`

TODO:
    Seed Content
